export interface Testimonial {
  text: string;
  author?: string;
}

export interface FAQItem {
  question: string;
  answer: string;
}

export interface Issue {
  title: string;
  description: string;
  icon: string;
}
